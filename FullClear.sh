#!/bin/bash

# Remove the Binaries folder
rm -rf "./Binaries"

# Remove the DerivedDataCache folder
rm -rf "./DerivedDataCache"

# Remove the Intermediate folder
rm -rf "./Intermediate"

# Remove the Saved folder
rm -rf "./Saved"
